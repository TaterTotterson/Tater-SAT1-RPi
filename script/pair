#!/bin/sh
set -eu

if [ "$(id -u)" -ne 0 ]; then
  printf '%s\n' "Run this command with sudo so it can restart the satellite service." >&2
  exit 2
fi

exec /opt/tater-sat1/venv/bin/tater-sat1-standalone \
  --config /etc/tater-sat1-standalone/config.toml \
  pair "$@"
